# 📘 完整版详细教程（用法二：直接使用完整项目文件）

> 适用：想要**掉线自动微信提醒**、或想长期维护项目、或想深度改文案/样式的服主。
> 全程可只用**浏览器**完成（GitHub 网页编辑），约 20 分钟，¥0。

---

## 0️⃣ 先认识这些文件（各管什么）

| 文件 | 作用 | 要不要改 |
| --- | --- | --- |
| `index.html` | 宣传站页面（名字/文案在这） | ✅ 必须改：把 `【服务器名】` 换成你的名字 |
| `js/main.js` | 配置：地址/版本/群号/官方加群链接 | ✅ 必须改：顶部 `SERVER`/`GROUP` |
| `css/style.css` | 外观颜色 | ⬜ 一般不用改 |
| `.watchdog/check.sh` | 掉线检测脚本 | ✅ 必须改：服务器地址 + 消息里的名字 |
| `.github/workflows/watchdog.yml` | 每 5 分钟自动执行哨兵 | ⬜ 不用改（想改频率可改 `cron`） |
| `配置向导.html` | 懒人生成工具 | ⬜ 用法二用不到，可忽略 |

---

## 1️⃣ 把方案复制到你自己的账号

1. 打开 https://github.com/upkea/mc-server-promo-template
2. 点绿色按钮 **Use this template**
3. Repository name 填英文名（如 `my-mc-server`），选 **Public** → **Create repository**
4. 稍等刷新，你的新仓库里已经有全部文件（含隐藏的 `.watchdog`、`.github`）

---

## 2️⃣ 改配置（三个文件，必改）

### 文件一：`js/main.js`（服务器信息）
在仓库列表点开 `js/main.js` → 点右上角 **✏️ 铅笔图标**（编辑）→ 改最上面这几行：

```js
var SERVER = {
  address: 'play.example.com:25565', // ★改成你的地址，如 play.yourserver.com:25565
  version: 'Java 版 1.20 – 1.21.8',  // ★改成你的版本
  group: ''                          // ★填你的 QQ 群号（纯数字）
};
var GROUP = {
  ...
  officialUrl: ''                    // ★可选：填官方加群链接（见 README）
};
```

改完点绿色 **Commit changes**（提交信息随便写，如 `update server info`）。

### 文件二：`.watchdog/check.sh`（掉线哨兵）
点开 `.watchdog/check.sh` → ✏️ 编辑，改 3 处：

1. 第 8 行左右：`SERVER="play.example.com:25565"` → 你的地址（**去掉 `#` 后面的注释也 OK**，保留也行）
2. 消息里两处 `【服务器名】`（在 🔴 掉线提醒 和 🟢 已恢复 的标题）→ 你的服务器名

改完 Commit changes。

### 文件三：`index.html`（网页上的名字）
页面里有多处 `【服务器名】`（标题/横幅/页脚），必须替换成你的名字。

**方法（网页编辑器没有"全部替换"）**：
1. 在 `index.html` 页面点 **Raw**（右上角，纯文本视图）
2. `Ctrl+A` 全选 → `Ctrl+C` 复制
3. 打开电脑**记事本** → `Ctrl+V` 粘贴 → `Ctrl+H` 查找替换：查找 `【服务器名】` 替换成你的服务器名 → **全部替换**
4. `Ctrl+A` → `Ctrl+C` 复制记事本内容
5. 回 GitHub：编辑 `index.html` → `Ctrl+A` 全选原有内容 → `Ctrl+V` 粘贴替换 → Commit changes

> 💡 嫌麻烦？也可以用仓库里的 `配置向导.html`：下载后双击 → 填你的信息 → 点生成下载 `index.html`，
> 然后把这个新 `index.html` **覆盖上传**到仓库（Add file → Upload files，覆盖同名文件即可），省得手动替换名字。
> 注意：那样会变成单文件版，`css/`、`js/` 文件夹留着不用删，不影响。

---

## 3️⃣ 开启网站（GitHub Pages）

1. 仓库页点 **Settings**（齿轮）
2. 左侧菜单找到 **Pages**
3. Build and deployment：Source 选 **Deploy from a branch** → Branch 选 **main**，目录 **/(root)** → **Save**
4. **等 1~3 分钟**，然后**获取网址**：
   - 回到 Settings → Pages，顶部绿色横幅：`Your site is live at https://你的用户名.github.io/仓库名/` ← **点它打开，这就是你的网站**
   - 或点仓库顶部 **Actions**，等 `pages build and deployment` 变绿 ✓ 后点进去也能看到
5. 打不开？见页面底部「常见问题」

---

## 4️⃣ 开启「掉线自动微信提醒」（哨兵）

1. **拿 SendKey**：打开 https://sct.ftqq.com → 微信扫码登录 → 关注「Server酱」服务号 → 复制 SendKey（`SCT` 开头）
2. 仓库点 **Settings** → 左侧 **Secrets and variables → Actions**
3. 点 **New repository secret**：
   - Name：`SERVERCHAN_KEY`
   - Secret：粘贴你的 SendKey
   - 点绿色 Add secret
4. 点仓库顶部 **Actions** 标签 → 左侧 **server-watchdog** → 若显示 **Enable workflow** 就点启用
5. 点 **Run workflow** 手动跑一次（右上角，绿色按钮）→ 等它变成 ✓
6. 点进这次运行看日志：出现 `状态: <首次> -> online` 和 `完成` 就成功了（首次只记录不发消息，正常）

之后**每 5 分钟**云端自动检测：
- 服务器掉线 → 微信收到「🔴 掉线提醒」（含提示去签到/开服）
- 服务器恢复 → 微信收到「🟢 已恢复」

---

## 5️⃣ 验证清单 ✅

- [ ] 打开你的网址：服务器名、地址、状态、加群按钮都正常
- [ ] Actions → server-watchdog 手动 Run 成功，日志有 `完成`
- [ ] 手机微信能收到 Server酱测试消息（可先在 Server酱网站发条测试）
- [ ] 想彻底验证掉线提醒：关掉服务器 → 手动 Run → 看日志出现 `>>> 发送掉线提醒` 且微信收到

---

## 6️⃣ 日常维护

| 想做什么 | 怎么做 |
| --- | --- |
| 改地址/群号/版本 | 网页编辑 `js/main.js` → Commit（1 分钟后网站自动更新） |
| 改文案/名字 | 网页编辑 `index.html` / `css/style.css` → Commit |
| 提醒不工作了 | 可能超 60 天没活动被暂停：仓库 Actions 页重新 **Enable**；或检查 Server酱 SendKey 是否还有效 |
| 想换域名 | 买便宜域名后：仓库 Settings → Pages → Custom domain 填域名 + 按提示加 DNS 记录 |
| 本地预览修改 | 用 `配置向导.html` 生成版，或本地双击 `index.html`（修改需电脑上编辑后重新上传） |

## ❓ 常见问题

- **页面显示 `【服务器名】`？** 第 2 步的文件三没做全，重新替换 `index.html` 里的占位符。
- **状态一直"离线"？** 检测走海外免费接口（已双接口交叉验证），部分国内服被误报，刷新/稍后再看。
- **哨兵日志显示"推送失败"？** 检查：SendKey 是否填错、`SERVERCHAN_KEY` 秘密名称是否完全一致。
- **忘了今天签到开服？** 掉线提醒会推微信，收到后去平台签到 + 点启动即可（平台机制不允许绕过签到自动开服）。
